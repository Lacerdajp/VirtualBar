import '../css/app.css'
import 'bootstrap/dist/js/bootstrap.esm'

document.getElementById('postagem').addEventListener('submit', function () {
  // Este script será executado quando o formulário for enviado
  location.reload();
});
