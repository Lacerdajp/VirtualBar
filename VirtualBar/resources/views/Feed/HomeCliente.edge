<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AdonisJS - A fully featured web framework for Node.js</title>
  @entryPointStyles('app')
  @entryPointScripts('app')
</head>
<body>


    @include('../Layouts/NavBar.edge')
    <main>
      <h1>Ola, {{user.nome}}! Bem vindo </h1>
      <div class="row">
      @if (estabelecimentos.length>0)
      @each(e in estabelecimentos)
      <div class="col-md-3">
        <div class="contact-box center-version">
            <a href="{{ route('estabelecimento.profile', { id: e.id }) }}">
                {{--  <img alt="image" class="img-circle" src="https://bootdey.com/img/Content/avatar/avatar1.png">  --}}
                <h3 class="m-b-xs"><strong>{{e.nome_estabelecimento}}</strong></h3>
                <div class="font-bold">
                  @each(item in e.tipo)
                    @if(item== e.tipo[e.tipo.length-1])
                    {{item}}
                    @else
                      {{item}} |
                      @end
                  @end
                </div>
                {{--  <address class="m-t-md">
                    <strong>Twitter, Inc.</strong><br>
                    795 Folsom Ave, Suite 600<br>
                    San Francisco, CA 94107<br>
                    <abbr title="Phone">P:</abbr> (123) 456-7890
                </address>  --}}
    
            </a>
        </div>
    </div>
      @end
    @else
      <p>Nenhum valor encontrado no banco de dados.</p>
    @end
      </div>
    @if(assoc.text=="Ocupado")
    <form  action="{{route('post.store')}}" method="POST"id="postagem">
        <div class="tab-content profile-tabs-content">
              <div class="tab-pane active" id="profile-overview" role="tabpanel">
                <div class="post-editor">
                    <textarea name="post" id="post" class="post-field" placeholder="Write Something Cool!"></textarea>
                    <div class="d-flex">
                        <button class="btn btn-success px-4 py-1" type="submit">Post</button>
                    </div>
                </div>
            </div>
                </div>
    </form>
    @end
    @if(posts)
      @each(item in posts)
      <div class="col-md-8">
        <div class="media g-mb-30 media-comment">
            <img class="d-flex g-width-50 g-height-50 rounded-circle g-mt-3 g-mr-15" src="https://bootdey.com/img/Content/avatar/avatar7.png" alt="Image Description">
            <div class="media-body u-shadow-v18 g-bg-secondary g-pa-30">
              <div class="g-mb-15">
                <h3  >{{item.estabelecimento}}</h3>
                @if(item.usuario==user.nome)
                  <form method="POST" action="{{route('post.destroy',{id:item.id},{qs:{_method:'DELETE'}})}}">
                    <button class="btn btn-danger w-50 py-2" type="submit" >Deletar Post</button>
                  </form>
                @endif
                <h5 class="h5 g-color-gray-dark-v1 mb-0">{{item.usuario}}</h5>
              </div>
              <p>{{item.text}}</p>

              {{--  <ul class="list-inline d-sm-flex my-0">
                <li class="list-inline-item g-mr-20">
                  <a class="u-link-v5 g-color-gray-dark-v4 g-color-primary--hover" href="#!">
                    <i class="fa fa-thumbs-up g-pos-rel g-top-1 g-mr-3"></i>
                    178
                  </a>
                </li>
                <li class="list-inline-item g-mr-20">
                  <a class="u-link-v5 g-color-gray-dark-v4 g-color-primary--hover" href="#!">
                    <i class="fa fa-thumbs-down g-pos-rel g-top-1 g-mr-3"></i>
                    34
                  </a>
                </li>
                <li class="list-inline-item ml-auto">
                  <a class="u-link-v5 g-color-gray-dark-v4 g-color-primary--hover" href="#!">
                    <i class="fa fa-reply g-pos-rel g-top-1 g-mr-3"></i>
                    Reply
                  </a>
                </li>
              </ul>  --}}
            </div>
        </div>
    </div>

      @end
     @end
     <form method="POST" action="{{route('user.destroy',{id:user.id},{qs:{_method:'DELETE'}})}}">
      <button class="btn btn-danger w-100 py-2" style="margin-bottom:15px" type="submit" >Deletar</button>
    </form>
    <form action="{{route('session.delete')}}" method="Get">
      <button class="btn btn-danger w-100 py-2"  type="submit" >Sair</button>
    </form>
  </main>
</body>
</html>
