<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AdonisJS - A fully featured web framework for Node.js</title>
  @entryPointStyles('profile')
  @entryPointScripts('profile')
</head>
<body class="bg-body-secondary">
  @include('../../Layouts/NavBar.edge')
  <div class="bg-white px-4 py-5 my-5 text-center">
    <img class="d-block mx-auto mb-4" src="/docs/5.3/assets/brand/bootstrap-logo.svg" alt="" width="72" height="57">
    <h1 class="display-5 fw-bold text-body-emphasis">{{estabelecimentos.nome_estabelecimento}}</h1>
    <div class="col-lg-6 mx-auto">
      <p class="lead mb-4">
        @if(estabelecimentos.tipo[0].length==1 )
        {{estabelecimentos.tipo}}
        @else
        @each(item in estabelecimentos.tipo)
          @if(item== estabelecimentos.tipo[estabelecimentos.tipo.length-1])
          {{item}}
          @else
            {{item}} |
            @end
        @end
        @end
      </p>
      <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
        @if(assoc.text=="Livre")
                <form action="{{route('associacao.store')}}" method="POST">
                  <input type="hidden" name="id" value="{{estabelecimentos.id}}"/>
                  <button class="btn btn-primary btn-lg px-4 gap-3" data-abc="true" type="submit">Entrar</button>
                </form>

                @elseif(assoc.text=="Ocupado")
                <form method="POST"
                action="{{
                  route('associacao.destroy', {id:assoc.cliente.id}, { qs: { _method: 'DELETE' } })
                }}"
              >
                  <button class="btn btn-primary btn-lg px-4 gap-3" style=" background-color: red;" data-abc="true" type="submit">Sair</button>
                  </form>
                @else
                {{--  <form action="{{route('associacao.store')}}" method="POST">
                  <input type="hidden" name="id" value="{{user.id}}"/>
                  <button class="m-t-10 waves-effect waves-dark btn btn-primary btn-md btn-rounded"  style=" background-color: orange;"data-abc="true" type="submit">Trocar para este Bar</button>
                </form>  --}}
                @end
      </div>
    </div>
  </div>


                {{--  <div class="row text-center m-t-20">
                    <div class="col-lg-4 col-md-4 m-t-20">
                        <h3 class="m-b-0 font-light">10434</h3><small>Articles</small>
                    </div>
                    <div class="col-lg-4 col-md-4 m-t-20">
                        <h3 class="m-b-0 font-light">434K</h3><small>Followers</small>
                    </div>
                    <div class="col-lg-4 col-md-4 m-t-20">
                        <h3 class="m-b-0 font-light">5454</h3><small>Following</small>
                    </div>
                </div>  --}}
                @if(assoc.text=="Ocupado")
                <form  action="{{route('post.store')}}" method="POST"id="postagem">
                  <div class="mb-2 alinhar-post">
                    <label for="TextArea" class="form-label"></label>
                    <textarea class="form-control"  name="post" id="post" rows="3" placeholder="Escreva alguma coisa"></textarea>
                    <button type="submit" class="btn btn-primary" style="float:right">Post</button>
                  </div>

                </form>
                @end
{{--
            </div>
        </div>  --}}

        @include('../../Layouts/ListarPosts.edge')
        {{--  @if(post)
        @each(item in post)
        <div class="col-md-8">
          <div class="media g-mb-30 media-comment">
              <img class="d-flex g-width-50 g-height-50 rounded-circle g-mt-3 g-mr-15" src="https://bootdey.com/img/Content/avatar/avatar7.png" alt="Image Description">
              <div class="media-body u-shadow-v18 g-bg-secondary g-pa-30">
                <div class="g-mb-15">

                  <h5 class="h5 g-color-gray-dark-v1 mb-0">{{item.usuario}}</h5>
                  <h6>{{item.data_update}}</h6>
                 @if(item.usuario==cliente.nome)
                    <form method="POST" action="{{route('post.destroy',{id:item.id},{qs:{_method:'DELETE'}})}}">
                      <button class="btn btn-danger w-20 py-2" type="submit" >Deletar Post</button>
                    </form>
                  @endif
                </div>
                <p>{{item.text}}</p>

                <ul class="list-inline d-sm-flex my-0">
                  <li class="list-inline-item g-mr-20">
                    <a class="u-link-v5 g-color-gray-dark-v4 g-color-primary--hover" href="#!">
                      <i class="fa fa-thumbs-up g-pos-rel g-top-1 g-mr-3"></i>
                      178
                    </a>
                  </li>
                  <li class="list-inline-item g-mr-20">
                    <a class="u-link-v5 g-color-gray-dark-v4 g-color-primary--hover" href="#!">
                      <i class="fa fa-thumbs-down g-pos-rel g-top-1 g-mr-3"></i>
                      34
                    </a>
                  </li>
                  <li class="list-inline-item ml-auto">
                    <a class="u-link-v5 g-color-gray-dark-v4 g-color-primary--hover" href="#!">
                      <i class="fa fa-reply g-pos-rel g-top-1 g-mr-3"></i>
                      Reply
                    </a>
                  </li>
                </ul>
              </div>
          </div>
      </div>

        @end
       @end  --}}

    {{--  </div>  --}}
{{--  </div>  --}}
</body>
</html>
